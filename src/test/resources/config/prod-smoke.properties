# Demo placeholder only; this must never be interpreted as production authorization.
# In a real project, configure prod-smoke explicitly through project policy and CI secrets.
baseUrl=https://www.saucedemo.com/
